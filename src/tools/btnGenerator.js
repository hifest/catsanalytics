function ObjectGenerator({ objects }) {
  return (
    <div>
      {/* {objects.map((object) => (
        <button 
            className={objects.class}  
            onClick={() => {objects.click}}
            >Зареєструватись</button>
      ))} */}
    </div>
  );
}

export default ObjectGenerator
